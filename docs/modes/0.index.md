# Modes

Leaflet-Geoman has different modes.
Only one mode can be active at a time. Enable them to let your users create and manipulate layers on the map.
You can enable them programatically, via the provided toolbar or by building your own toolbar.
